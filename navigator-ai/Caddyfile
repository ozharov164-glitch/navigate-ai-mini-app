{$PUBLIC_HOST} {
    handle /bot/webhook* {
        reverse_proxy bot:8081
    }
    handle /api/* {
        reverse_proxy backend:8000
    }
    handle /health {
        reverse_proxy backend:8000
    }
    respond "NavigAI API — use /api/*" 404
}
